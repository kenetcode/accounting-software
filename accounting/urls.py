from django.urls import path
from . import views
app_name = 'accounting'
urlpatterns = [
    #path('login/', views.login, name='login'), # este path es para la vista de login modificar cuando ya se tenga la vista de login en el archivo views.py de la aplicacion accounting 
]